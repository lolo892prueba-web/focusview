name = "focusview"
compatibility_date = "2023-12-01"
pages_build_output_dir = "."

[[pages_build_config]]
# Build command and output directory
build_command = "echo 'Static site ready'"
destination_dir = "."

# Environment variables
[vars]
# Add any environment variables if needed

# Routes
[[pages_build_config.routes]]
pattern = "/api/*"
# For API routes, you might need to proxy to your Flask app
# But for static sites, this is optional
